# copy to .env and fill values
PORT=5000

# SMTP settings - example for Gmail SMTP; alternative: use an SMTP provider (SendGrid, Mailgun, SES, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=youremail@gmail.com
SMTP_PASS=your_email_app_password_or_smtp_password

# Where contact form should be sent (company inbox)
TO_EMAIL=sayali@mammothpharmatech.com
FROM_LABEL="Mammoth Website"
